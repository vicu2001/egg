dineroCofla = prompt("Cuanto dinero tienes Cofla?");
dineroRoberto = prompt("Cuanto dinero tienes Roberto?");
dineroPedro = prompt("Cuanto dinero tienes Pedro?");
if (dineroCofla >= 0,6 && dineroCofla < 1);
{alert ("comprate el helado de agua")};
if (dineroRoberto >= 0,6 && dineroCofla < 1);
{alert ("comprate el helado de agua")};
if (dineroPedro >= 0,6 && dineroCofla < 1);
{alert ("comprate el helado de agua")};

elseIf (dineroCofla >= 1 && dineroCofla < 1,6);
{alert ("comprate el palito helado de crema")};
elseIf (dineroRoberto >= 1 && dineroRoberto < 1,6);
{alert ("comprate el palito helado de crema")};
elseIf (dineroPedro >= 1 && dineroPedro < 1,6);
{alert ("comprate el palito helado de crema")};

elseIf (dineroCofla >= 1,6 && dineroCofla < 1,7);
{alert ("comprate el helado marca heladix")};
elseIf (dineroRoberto >= 1,6 && dineroRoberto < 1,7);
{alert ("comprate el helado marca heladix")};
elseIf (dineroPedro >= 1,6 && dineroPedro < 1,7);
{alert ("comprate el helado marca heladix")};

elseIf (dineroCofla >= 1,7 && dineroCofla < 1,8);
{alert ("comprate el helado marca heladovich")};
elseIf (dineroRoberto >= 1,7 && dineroRoberto < 1,8);
{alert ("comprate el helado marca heladovich")};
elseIf (dineroPedro >= 1,7 && dineroPedro < 1,8);
{alert ("comprate el helado marca heladovich")};

elseIf (dineroCofla >= 1,8 && dineroCofla < 2,9);
{alert ("comprate el helado marca helardo")};
elseIf (dineroRoberto >= 1,8 && dineroRoberto < 2,9);
{alert ("comprate el helado marca helardo")};
elseIf (dineroPedro >= 1,8 && dineroPedro < 2,9);
{alert ("comprate el helado marca helardo")};

elseIf (dineroCofla >= 1,8 && dineroCofla < 2,9);
{alert ("comprate el helado marca helardo")};
elseIf (dineroRoberto >= 1,8 && dineroRoberto < 2,9);
{alert ("comprate el helado marca helardo")};
else if (dineroPedro >= 1,8 && dineroPedro < 2,9);
{alert ("comprate el helado marca helardo")};

else if (dineroCofla >= 2,9)
{alert ("comprate el 1/4 kg o el potecito de helado con confites")};
else if (dineroRoberto >= 2,9){
alert ("comprate el 1/4 kg o el potecito de helado con confites")};
else if (dineroPedro >= 2,9);
{
    alert("comprate el 1/4 kg o el potecito de helado con confites")};




 // >